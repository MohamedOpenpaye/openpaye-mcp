services:
  - type: web
    name: openpaye-mcp
    env: node
    plan: free
    buildCommand: "npm install"
    startCommand: "node server.mjs"
    envVars:
      - key: NODE_ENV
        value: production
